<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ContentLoadingProgress">
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CardMultilineWidget">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="editTextColor">@color/inputText</item>
        <item name="android:editTextColor">@color/inputText</item>
        <item name="colorControlNormal">@color/inputText</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="android:textColorHint">@color/hintText</item>
        <item name="android:textCursorDrawable">@color/colorAccent</item>
    </style>

</resources>
